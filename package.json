{
  "name": "scanya",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @scanya/web run dev",
    "dev:api": "npm --workspace @scanya/api run dev",
    "build": "npm run build --workspace @scanya/shared && npm run build --workspace @scanya/api && npm run build --workspace @scanya/web",
    "typecheck": "npm run typecheck --workspace @scanya/shared && npm run typecheck --workspace @scanya/api && npm run typecheck --workspace @scanya/web"
  }
}
